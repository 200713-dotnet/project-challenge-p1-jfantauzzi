@{
  Layout = "_Layout";
}

<h2>Pizza List</h2>
<ul>
  @foreach (var p in @Model)
  {
    <li>@p</li>
  }
</ul>

<form action=""> PizzaViewModel
  display the crust
  display the sizes
  display the toppings
</form>